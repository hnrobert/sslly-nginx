# Example configuration file for sslly-nginx
# Copy this file to config.yaml and modify according to your needs

# Format: port: [domains]
# The key is the local port that the application listens on
# The value is a list of domains that should be proxied to that port

1234:
  - a.com
  - b.a.com

5678:
  - b.com
